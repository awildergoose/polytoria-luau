import type { Color } from "../types/color";
import type { DynamicInstance } from "./dynamic_instance";

export declare class SunLight extends DynamicInstance {
	Brightness: number;
	Color: Color;
	Shadows: boolean;
}
